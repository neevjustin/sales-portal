# Set to 'development' to use SQLite or 'production' to use PostgreSQL
ENV=development

# --- PostgreSQL Settings (used only if ENV=production) ---
POSTGRES_USER=your_pg_user
POSTGRES_PASSWORD=your_pg_password
POSTGRES_SERVER=localhost:5432
POSTGRES_DB=sales_portal
